/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['https://lh3.googleusercontent.com', 'https://firebasestorage.googleapis.com'],
  },
}

module.exports = nextConfig
