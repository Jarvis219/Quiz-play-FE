export enum EButtonSize {
  large = 'large',
  middle = 'middle',
  small = 'small',
}

export enum EButtonType {
  primary = 'primary',
  default = 'default',
  dashed = 'dashed',
  text = 'text',
  link = 'link',
}
